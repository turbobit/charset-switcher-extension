{
  "name": "charset-switcher-extension",
  "version": "1.1.0",
  "description": "Chrome extension for forcing webpage charset",
  "author": "",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "clean:dist": "node scripts/clean-dist.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "package:chrome": "node scripts/package-extension.mjs",
    "i18n:check": "node scripts/validate-locales.mjs",
    "release": "npm run clean:dist && npm run package:chrome && npm run build:pages",
    "build": "npm run release",
    "deploy:pages-ci": "gh workflow run pages.yml --ref main",
    "build-and-deploy": "npm run build && npm run deploy:pages-ci"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "keywords": [
    "chrome-extension",
    "charset",
    "encoding",
    "utf-8",
    "euc-kr",
    "internationalization"
  ],
  "manifest": "manifest.json"
}